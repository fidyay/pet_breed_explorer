export type TWeight = {
  imperial: string;
  metric: string;
};

export type TImage = {
  id: string;
  width: number;
  height: number;
  url: string;
};

export type THeight = {
  imperial: string;
  metric: string;
};
